# 2026-02-26

## feat: order management improvements (admin)

- Replaced "Origen" column in orders table with stops count (non-dropoff)
- Created `PlacesAutocompleteInput` component (Google Places, restricted to Costa Rica)
- Created `EditStopAddressDialog` for setting/editing stop addresses
- Created `useCalculateDistance` and `useUpdateStop` hooks
- Added `calculateDistance` and `updateStop` methods to `OrderService`
- Added "Calculate Distance" button on order detail (shows when distance is null + all stops have addresses)
- Added edit/add address buttons on each stop in order detail page
