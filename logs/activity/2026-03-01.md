# 2026-03-01

## 21:57 — feat: stop management, quote line items, and reconciliation UI

- `AddStopDialog`: add stops with map picker, type selector, contact fields
- `EditStopDetailsDialog`: edit stop instructions and contact info
- `QuoteLineItemsEditor`: reusable per-stop line items editor
- `CreateQuoteDialog`: integrate line items with stop grouping
- `QuoteDetailDialog`: display line items grouped by stop
- `ReconciliationDialog`: reconcile actual costs vs original quote
- New hooks: `useCreateStop`, `useReconcileOrder`
- Synced generated types and enums (Failed status)

## 20:39 — feat: date-based schedule UI + enum-driven status labels

- Refactored `DriverScheduleTab` to date-based entries (removed day-of-week/overrides)
- Replaced raw `t()` status calls with `statusLabel()`/`actionLabel()` helpers across all pages
- Added `ReasonBadge`/`UrgencyBadge` for needs-attention page
- Extracted `resolveStopAddress()` utility to deduplicate 6x address resolution pattern
- Moved Purchase→pickup type mapping into `StopMarker` (`isPickupType` helper)
- Removed dead `orderStatusLabel()` passthrough

## 16:10 — refactor: migrate calendar to FullCalendar + app-timezone utilities

- Replaced Schedule-X with FullCalendar for driver schedule calendar
- Added timezone-aware date utilities (`toAppTzComponents`, `getTodayAppTz`, `toDateString`)
- All date formatting uses `America/Costa_Rica` regardless of device/server timezone

## 15:19 — fix: show "Scheduled" instead of "Override" on calendar events

## 15:06 — revert: "no description" fallback on stops

- Reverted `f4e4cf4` — italic "No description provided" fallback on pickup/purchase stops

## 14:34 — fix: move address-required banner above card grid

## 14:15 — feat: address-required banner before quoting

- Amber info banner in Quote History when any stop is missing an address

## 07:16 — refactor: migrate raw t() calls to actionLabel()

- Replaced `t('common:save')`, `t('common:cancel')`, etc. with `actionLabel()` across all pages
- Updated `actionLabel()` to read from nested `common.actions.*` namespace
