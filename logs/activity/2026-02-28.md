# 2026-02-28

## feat: needs-attention page and dashboard integration (admin)
- Added `/needs-attention` page with urgency filters, summary bar, and order cards
- Created UrgencyBadge, ReasonBadge, NeedsAttentionCard, CancelOrderDialog components
- Added `useNeedsAttention` hook with 30s polling and `useCancelOrder` mutation
- Updated `useChangeTier` to show dispatch result toasts after auto-re-dispatch
- Added sidebar nav item with red badge count for critical+high orders
- Added dashboard alert card when orders need attention
- Updated `orderService.ts` with `getNeedsAttention()` and `cancelOrder()` methods

## feat: driver schedule calendar UI (admin)

- Replaced day-by-day `DriverScheduleTab` with Schedule-X calendar (week + month views)
- Created `buildCalendarEvents()` utility — transforms templates/overrides into calendar events
- Created `ScheduleEventDialog` — create/edit overrides, mark unavailable, revert to template
- Past dates are read-only (gray styling, toast on click)
- Added `Collapsible` component for weekly template editor below calendar
- Updated `useSyncOverrides`/`useSyncSchedules` hooks to handle `affectedOrders` response
- Updated `driverService.ts` — new return types with `affectedOrders` for cascade awareness
- Toast notifications for reassignment results
- Added `temporal-spec/global` types to `tsconfig.json`
