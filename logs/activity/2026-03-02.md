# 2026-03-02

## feat: reconciliation tab in needs-attention page (admin)

- Added Conflicts/Reconciliation tabs to needs-attention page
- Created `PendingReconciliationCard` component for reconciliation queue
- Created `usePendingReconciliation` hook with 30s stale time
- Sidebar badge now combines urgent + reconciliation counts with color coding
- Wired broadcast invalidation for reconciliation query
- Extracted `useOrderCurrencySymbol` hook to deduplicate currency resolution (was in 3 places)
- Replaced redundant `reconciliationCount` state with `reconciliationOrders.length`
- Fixed `NeedsAttentionCard` hardcoded stop/stops to use translation key
