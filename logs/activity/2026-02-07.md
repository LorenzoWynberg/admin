# Activity Log - 2026-02-07

## Route Management — Admin Dispatch Board

### 10:30 - feat: add route service, hooks, and sidebar nav

- Created RouteService with full API: list, getById, create, update, destroy, addStop, removeStop, reorderStops, unassignedStops
- Created React Query hooks: useRouteList, useRoute, useRouteMutations (6 mutations), useUnassignedStops
- Added route nav item to Sidebar (using Route icon from lucide-react)
- Synced types/generated.d.ts and regenerated app-enums.ts

### 11:00 - feat: add dispatch board with map and drag-drop

- Created DispatchBoard (main split-panel layout with date state, DndContext, route selection)
- Created DateNavigator (prev/next day buttons + calendar popover)
- Created CreateRouteDialog (name + driver + notes form)
- Created RouteCard (name, driver select, sortable stops, delete)
- Created SortableStopCard (draggable stop with grip handle, type icon, address, remove button)
- Created UnassignedStopsList (droppable area with stop cards)
- Created AddOrderDialog (search unassigned orders to add to route)
- Created RouteMap (Google Maps with colored markers for route stops and grey for unassigned)
- Created StopMarker (AdvancedMarker with pickup/dropoff/unassigned colors)
- Created RouteStatusBadge (status badge with color mapping)
- Created routes/page.tsx (full-height dispatch board)
- Installed dependencies: @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities, @vis.gl/react-google-maps
- Added shadcn calendar component

### 12:00 - fix: payment section loads all orders instead of specific order

- Fixed PaymentService.getByOrderPublicId to use `?order={publicId}` query param instead of `?filter[order_id]={numericId}`
- Updated useOrderPayments hook to accept `orderPublicId: string` instead of `orderId: number`
- Updated PaymentSection component and order detail page to pass `order.publicId`

### 12:30 - feat: quote history with rejection feedback on order detail

- Added `quotes` collection to OrderData DTO (API) and eager loaded in OrderController.show()
- Replaced single "Quote & Payment" card with full Quote History section
- Shows all quotes sorted newest first with status badge, version, total, and link to detail
- Current quote highlighted with primary border
- Rejection reason shown inline with amber banner per rejected quote
- Also added rejection reason display on quote detail page

### 13:00 - refactor: replace top banner with quote detail modal

- Removed standalone rejection reason banner from order detail header (redundant with inline display)
- Created QuoteDetailDialog component with pricing breakdown, schedule, notes, and rejection reason
- Quote history cards now open a modal instead of navigating to the quote page
