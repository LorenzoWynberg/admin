# 2026-02-13

## 11:14 — feat: support translated notification payloads

- Updated `NotifData` interface with `translationKey`, `translationParams`, and `message` fields
- `getTitle()` now checks `translationKey` first, falls back to `message` (backward compat), then CRUD pattern
- `getMessage()` shows model context for translated notifications, falls back to CRUD pattern

## 01:30 - feat: add service window settings page

- Created `settingService.ts` with `getServiceWindow()` and `updateServiceWindow()`
- Created `useServiceWindow` query hook and `useUpdateServiceWindow` mutation hook
- Added Service Window card (Clock icon) to settings hub page
- Created `/settings/service-window` config page with enable/disable switch, time inputs, visual 24h timeline
- Synced types from API (`generated.d.ts`) with new `SettingData` and `UpdateSettingData`
