# 2026-02-27

## feat: map-based address picker for stop editing (admin)

- Created `GeoService` with `reverseGeocode()` wrapper for `/geo/reverse-geocode` endpoint
- Created `MapAddressPicker` component — interactive map with search, draggable marker, click-to-place, and reverse geocode
- Exported `PlacesAutocompleteInner` from `PlacesAutocompleteInput` for embedding inside external `APIProvider`
- Upgraded `EditStopAddressDialog` to use `MapAddressPicker` instead of text-only autocomplete input
- Dialog widened to `max-w-2xl` to accommodate the map
