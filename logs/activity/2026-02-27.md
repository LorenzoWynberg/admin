# 2026-02-27

## fix: disable Save button when service window form is clean (admin)

- Added `isDirty` state tracking comparing form values against initial data
- Save button now disabled when no changes have been made

## feat: map-based address picker for stop editing (admin)

- Created `GeoService` with `reverseGeocode`, `autocomplete`, and `placeDetails` wrappers (backend-proxied)
- Created `MapAddressPicker` — center-pin pattern (fixed CSS overlay, map moves underneath) matching client's golden standard
- Custom `SearchBar` using backend-proxied autocomplete (not Google's native widget)
- Pin lift animation on drag (120ms up, 220ms settle) with shadow effect
- POI click support — clicking places on the map selects them
- Disabled scroll zoom (cursor-biased zoom shifts center); uses Google's +/- controls instead
- Removed fullscreen and Street View controls for cleaner UI
- Upgraded `EditStopAddressDialog` to use `MapAddressPicker`, widened to `sm:max-w-3xl`
- Fixed API response wrapper unwrapping in `GeoService` (`.items`/`.item` from `Multiple<T>`/`Single<T>`)

## feat: smart dispatch admin UI — phases 1C/D, 3, 4, 5D (admin)

- **Driver detail page**: Added tabs (Details | Schedule), base location card with `MapAddressPicker`
- **DriverScheduleTab**: Weekly schedule management (day-by-day time blocks) + date overrides with available/unavailable toggle
- **Driver hooks**: `useDriverSchedules`, `useSyncSchedules`, `useSyncOverrides`
- **Change tier**: `useChangeTier` hook + `changeTier()` service method
- **CreateQuoteDialog**: Tier adjustment callout when feasibility is RED and tier can be downgraded
- **Outsource**: `useOutsourceOrder` hook + `outsource()` service method + outsource button on order detail page
- Installed `@schedule-x/react`, `@schedule-x/calendar`, `@schedule-x/theme-default` (schedule-x packages)
